# Central variables for pfs-infinity
# Ports only; VENV_PATH is defined at repo root (../../Justfile.vars)

# Networking ports
WS_PORT := "8811"
QUIC_PORT := "8853"
REDIS_PORT := "6389"
REGISTRY_PORT := "5000"
BACKEND_PORT := "8811"

# Bind address for backend (default: listen on all interfaces)
BIND_ADDR := "0.0.0.0"
